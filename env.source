export PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/etc/mininit/bin
export TERM=linux
ttylog() {
    log="$1"
    echo "% $log" | tee -a /var/log/mininit/services.log
}
timelog() {
    timelog="$1"
    svc="$2"
    echo "$svc took $timelog" >> /var/log/mininit/timeblame.log
}
